dependencies {
    compile libs.bcpg
    compile libs.commons_compress
}

modifyPom { pom ->
    pom.project {
        name "Pkg4j core module"
        description "Library for creating OS specific packages."
    }
}
