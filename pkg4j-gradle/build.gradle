apply plugin: "groovy"

dependencies {
    compile     project(':pkg4j-core')
    compile     gradleApi()
    compile     libs.comonsLang
    compile     libs.jdeb
}

modifyPom { pom ->
    pom.project {
        name "Pkg4j gradle plugin"
        description "Gradle plugin for creating OS specific packages."
    }
}

